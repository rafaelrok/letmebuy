spring.profiles.active=${APP_PROFILE:dev}
server.port=9099

spring.jpa.open-in-view=true

spring.datasource.hikari.maximum-pool-size=5

default.sender=
default.recipient=

security.client-id=${CLIENT_ID:letmebuy}
security.client-secret=${CLIENT_SECRET:letmebuy123}

#jwt.secret=${JWT_SECRET:MY-JWT-SECRET}
security.jwt.duration=${JWT_DURATION:86400}

cors.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}

spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:rafaelvieiradev@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:@87046548rF@}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

email.password-recover.token.minutes=${PASSWORD_RECOVER_TOKEN_MINUTES:30}
email.password-recover.uri=${PASSWORD_RECOVER_URI:http://localhost:5173/recover-password/}

aws.access_key_id=${AWS_KEY:empty}
aws.secret_access_key=${AWS_SECRET:empty}
s3.bucket=${LETMEBUY_BUCKET_NAME:empty}
s3.region=${BUCKET_REGION:sa-east-1}

img.prefix.client.profile=cp
img.profile.size=200

spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2MB
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

#spring.main.allow-bean-definition-overriding=true

logging.level.com.rafaelvieira.letmebuy.controllers=DEBUG

spring.jackson.serialization.fail-on-empty-beans=false
